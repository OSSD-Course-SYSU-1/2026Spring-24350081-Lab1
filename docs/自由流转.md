# 自由流转 — 《移动应用开发》课程总结

> 来源：`02.自由流转.pptx`  
> 适用范围：HarmonyOS NEXT Release 及以上版本

---

## 一、自由流转概述

自由流转是 HarmonyOS 的核心能力之一，旨在打破设备壁垒，实现**多设备协同、数据共享、硬件互助**。它涵盖两种基本形态：

| 形态 | 说明 |
|------|------|
| **同源/异源投屏** | 将内容镜像到另一屏幕 |
| **多端协作 + 硬件互助** | 设备间协同工作、共享硬件能力 |

### 使用场景（5 种流转）

| 类型 | 示例 |
|------|------|
| **音乐流转** | 手机播放音乐，流转到音箱 |
| **视频流转** | 手机看视频，流转到电视 |
| **应用流转** | App 跨设备无缝迁移 |
| **文件流转** | 文件跨设备快速传输 |
| **数据流转** | 用户数据跨设备同步 |
| **硬件流转** | 调用另一设备的相机、屏幕等硬件 |

---

## 二、自由流转的两种使用模式

### 同时使用（Simultaneous）— 多端协同

| 维度 | 说明 |
|------|------|
| **并发（Concurrent）** | 多个设备同时参与同一任务 |
| **协作（Collaboration）** | 多设备分工合作 |
| **互补（Complementarity）** | 设备互相补充能力短板 |

### 相继使用（Sequential）— 跨端迁移

| 维度 | 说明 |
|------|------|
| **连续（Continuity）** | 任务在不同设备间连续执行 |
| **一致（Consistent）** | 跨设备体验和界面风格一致 |
| **自适应布局（Adaptive UI）** | UI 根据设备形态自动调整 |

### 关键概念

| 概念 | 定义 |
|------|------|
| **同步（Synchronization）** | 用户数据跨设备同步 |
| **迁移（Migration）** | 用户行为/任务从一台设备迁移到另一台，业务不中断 |
| **镜像（Mirror）** | 相同内容镜像到不同屏幕 |
| **分裂（Division）** | 设备分裂为独立布局，各设备协作交互 |
| **合并（Merging）** | 多设备意图整合 |
| **延展（Extension）** | 交互从一台设备延展到另一台 |
| **遥控（Remote Control）** | 一台设备控制另一台 |

---

## 三、传统开发痛点

传统 OS 开发分布式应用面临以下挑战：

- **设备连接复杂** — 多种连接协议并存，发现与通讯困难
- **数据互通难** — 无法轻易获知数据在哪台设备哪个应用内
- **硬件无法协同** — 大设备硬件资源无法复用，小设备塞不进更多硬件
- **安全认证** — 设备间安全与认证机制不统一
- **生命周期调度** — 跨设备应用生命周期调度缺失

```
传统模式：
设备A [App/UI/通信/数据/文件] ←→ 设备B [App/UI/通信/数据]
                  数据读取 / 文件传输 / 发现连接传输

痛点：每项能力都要应用自己实现，工作量大
```

---

## 四、分布式应用开发框架（四大能力）

HarmonyOS 从系统底层打通 1+8+N 互联互通能力，对外提供 **硬件、数据、应用** 三个维度的跨设备访问。

```
┌──────────────────────────────────────────────────┐
│                  分布式应用                        │
├────────────┬───────────┬──────────┬─────────────┤
│ 分布式软总线 │ 分布式任务调度 │ 分布式数据 │ 分布式硬件 │
└────────────┴───────────┴──────────┴─────────────┘
```

### 4.1 分布式软总线

> 解决设备之间**互联互通**痛点

| 项目 | 说明 |
|------|------|
| **关键技术** | 软硬协同、极简协议栈、智能调度 |
| **生态价值** | 提供**安全、易用、高效、可靠**的传输通道 |

### 4.2 分布式数据

> 提供**跨设备数据访问**能力

| 项目 | 说明 |
|------|------|
| **关键技术** | 分布式数据库（持久数据）、分布式数据对象（临时数据） |
| **生态价值** | 提供跨设备数据共享访问能力 |
| **典型使用** | `distributedDataObject` 实现过程数据的分布式共享访问（跨端迁移、多端协同） |
| **竞品特性** | 播控中心、跨设备拖拽、跨设备剪贴等 |

**层级结构：**

```
应用层 → 框架层（分布式软总线）
              ├─ 分级 / 加密 / 权限控制
              ├─ 并发访问 / 生命周期 / 统一数据代理 / 故障恢复
              └─ 存储管理 / 运行管理 / 数据安全
通信层：组网 → 发现 → 连接 → 传输 → 认证 → 同步管理
```

### 4.3 分布式硬件

> 提供**跨设备硬件共享访问**能力

| 项目 | 说明 |
|------|------|
| **关键技术** | 硬件虚拟化、硬件同步技术 |
| **生态价值** | 提供简单、易用的跨设备硬件共享 |
| **使用方式** | `getSupportedCameras()` 获取分布式相机列表；`createCameraInput()` 操作对端相机 |

### 4.4 分布式任务调度

> 提供**应用跨端迁移**和**多端协同**能力

| 能力 | 关键方法 | 说明 |
|------|----------|------|
| **跨端迁移** | `onContinue()` → `onCreate()` | 源端保存状态，对端恢复 |
| **多端协同** | `startAbility()`、`startAbilityByCall()` | 跨设备启动/建立协同关系 |

---

## 五、跨端迁移开发实战

### 5.1 约束和限制

- 设备需 HarmonyOS NEXT Release 及以上版本
- 双端登录**同一华为账号**
- 双端打开 **Wi-Fi 和蓝牙**（推荐同局域网加速）
- 只能在同应用（UIAbility）之间触发
- `wantParam` 传小数据（<100KB），大数据用分布式数据对象

### 5.2 权限配置

在 `entry/src/main/module.json5` 中配置 `continuable: true`：

```json
{
  "module": {
    "abilities": [
      {
        "continuable": true
      }
    ]
  }
}
```

> `false` 表示不支持迁移，默认为 `false`。

### 5.3 迁移数据方式

| 数据类型 | 迁移方式 |
|----------|----------|
| **内存数据** | 通过**分布式数据对象**（`distributedDataObject`） |
| **文件/大数据** | 通过**分布式文件**（`commonType.Asset` + 分布式数据对象） |

### 5.4 迁移三步曲

```
① 权限配置（module.json5）
② 源端 onContinue() 中保存数据
③ 对端 onCreate() / onNewWant() 中恢复数据
```

### 5.5 场景一：分布式数据对象迁移

**源端（设备 A）：**

```typescript
async onContinue(wantParam: Record<string, Object>): Promise<AbilityConstant.OnContinueResult> {
  // 1. 创建分布式数据对象
  this.d_object = distributedDataObject.create(this.context, source);
  // 2. 生成组网 id，激活分布式数据对象
  let dataSessionId: string = distributedDataObject.genSessionId();
  this.d_object.setSessionId(dataSessionId);
  // 3. 将组网 id 传递到对端
  wantParam['dataSessionId'] = dataSessionId;
  // 4. 持久化数据对象（确保对端能恢复）
  await this.d_object.save(wantParam.targetDevice as string);
}
```

**对端（设备 B）：**

```typescript
handleDistributedData(want: Want) {
  // 1. 创建空分布式数据对象
  this.d_object = distributedDataObject.create(this.context, remoteSource);
  // 2. 获取组网 id
  dataSessionId = want.parameters.dataSessionId as string;
  // 3. 监听状态恢复
  this.d_object.on("status", (sessionId, networkId, status) => {
    if (status == 'restored') {
      // 数据已同步，可以读取
    }
    this.d_object.setSessionId(dataSessionId);
  });
}
```

### 5.6 场景二：文件资产迁移

**源端：**

```typescript
// 1. 将文件放入 distributedFilesDir 目录
// 2. 创建 Asset 资产对象
let attachment: commonType.Asset = {
  name: fileName,
  uri: distributedUri,
  path: filePath,
  createTime: ctime,
  modifyTime: mtime,
  size: size,
};
// 3. 将 Asset 作为分布式数据对象的根属性进行迁移
this.d_object = distributedDataObject.create(this.context, source);
```

**对端：**

```typescript
// 1. 创建空的 Asset 对象作为占位
let attachment: commonType.Asset = { name: '', uri: '', ... };
// 2. 创建分布式数据对象
this.d_object = distributedDataObject.create(this.context, source);
// 3. 监听 status == 'restored' 时，资产数据同步完成即可读取
this.d_object.on("status", (sessionId, networkId, status) => {
  if (status == 'restored') {
    // 可获取 this.d_object['attachment']
  }
});
```

---

## 六、迁移总结

| 要点 | 说明 |
|------|------|
| **核心原则** | 分析业务数据模型，采用合适的数据同步能力 |
| **迁移框架能力** | 通过支持迁移的组件和页面栈迁移参数配置 |
| **分布式数据对象** | 适合**内存数据** |
| **分布式文件** | 适合**文件数据** |
| **性能建议** | 仅传递**应用状态相关**的数据，避免传递大量冗余数据 |

---

## 七、课程总结

| 模块 | 内容 |
|------|------|
| **自由流转体验** | 跨端迁移、多端协同 |
| **流转开发挑战** | 连接复杂、数据互通、交互协同 |
| **流转应用开发框架** | 分布式软总线、分布式数据、分布式硬件、分布式任务调度 |
| **跨端迁移开发** | 配置/权限、源端（onContinue）、对端（onCreate）、数据迁移方式选型 |

---

## 八、互动讨论

- 如何在具体业务中平衡**同步数据量**和**迁移启动速度**？
- 当出现网络波动时，如何保证迁移的**可靠性和用户体验**？
- 文件资产的**安全隔离**如何实现？
