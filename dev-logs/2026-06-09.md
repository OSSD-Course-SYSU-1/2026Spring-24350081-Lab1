# 开发日志 — 2026-06-09

## 完成事项

- [x] 修复手机 UI 断点检测 px/vp 混淆
  - 根因：`windowRect.width` 返回物理像素，`calcBreakpoint` 期望 vp，手机被误判为 LG（PC布局）
  - 修复：用 `display.getDefaultDisplaySync().densityPixels` 转换 px→vp
  - 文件：Index.ets

- [x] 恢复 ClipCard `width: '100%'`
  - 多端部署提交中移除了此属性，导致卡片在手机单列布局中不填满宽度
  - 修复：恢复 `.width('100%')` 到 ClipCard 外层 Column
  - 文件：ClipCard.ets

- [x] 修复 SettingsPage 手机布局缺水平边距
  - `buildSingleColumnContent` wrapper Column 缺少 `padding({ left: 16, right: 16 })`
  - 原代码的卡片 `margin({ left: 16, right: 16 })` 在抽取共享 Builder 时丢失
  - 文件：SettingsPage.ets

- [x] 平板端 UI 不响应数据变化 — @Builder 内联重构
  - 根因：ArkUI V2 中，ForEach + key 复用 DOM 节点时，@Builder 不会重新执行
  - `buildClipGrid()` / `buildSingleColumnList()` / `buildCategoryGrid()` / `buildDualColumnContent()` 等 @Builder 包裹的布局在数据变更后 UI 不刷新
  - 修复方案（按 CLAUDE.md 推荐）：将所有内容布局从 @Builder 方法中**内联**到 `build()` 中
  - 改动文件：
    - ClipboardPage.ets：内联 `buildSingleColumnList` / `buildClipGrid`，移除对应 @Builder
    - CategoryPage.ets：内联 `buildSingleColumnList` / `buildCategoryGrid` / `buildCategoryCard`，移除对应 @Builder
    - SettingsPage.ets：内联 `buildSingleColumnContent` / `buildDualColumnContent`，移除对应 @Builder
  - 保留下来的 @Builder 方法均为直接调用（buildEmptyState、buildActionSheet、buildMoveDialog 等），不受 ForEach key 复用影响

## 待办

- [ ] 平板模拟器编译验证
- [ ] 平板模拟器：确认数据变更（置顶/删除/移动/搜索）后 UI 即时刷新
- [ ] 平板模拟器：确认 Tab 切换后数据一致
- [ ] 平板模拟器：确认性能改善（不再"响应极其缓慢"）

---

## 补充（同日）：平板/PC 响应缓慢修复 v2

- [x] 新增 `@Trace dataVersion` 计数器保障响应式链路
  - 根因分析：平板布局使用条件渲染 `if/else`，与手机的 `Tabs` 容器不同。ArkUI V2 中，条件渲染下的 `@ComponentV2` 通过 `@Param` 访问 `@ObservedV2` 对象的 `@Trace` 属性时，响应式依赖可能不稳固
  - 修复：在 `ClipboardViewModel` 中新增 `@Trace dataVersion: number`，每次 `applyFilters()` 和 `deleteAll()` 后递增；在 `ClipboardPage.build()` 开头用 `void(this.vm.dataVersion)` 显式注册依赖
  - 文件：ClipboardViewModel.ets、ClipboardPage.ets

- [x] 平板/PC 网格布局重构：嵌套 ForEach → Flex + FlexWrap.Wrap
  - 根因：旧布局用 `Row > ForEach(distributeToColumns()) > Column > ForEach(clips)` 嵌套 ForEach。当 item 在列间迁移（如置顶后从第 2 列跳到第 1 列），ArkUI V2 的 ForEach diff 不稳定，导致 UI 不刷新或极其缓慢
  - 修复：改为 `Flex({ wrap: FlexWrap.Wrap }) > ForEach(clips)` 单层 ForEach，每张卡片用 `width: '50%'` / `'33.33%'` 百分比宽度，Flex 自动换行处理多列。消除嵌套 ForEach 和 `distributeToColumns()` 方法
  - 优势：与手机端已验证的单层 ForEach 模式一致，响应式链路可靠
  - 文件：ClipboardPage.ets

- [x] 移除废弃的 `distributeToColumns()` 方法，替换为 `getCardWidth()`

## 技术要点

- `Flex + FlexWrap.Wrap` 是替代手动列分配的更可靠方案，单层 ForEach 避免了 ArkUI V2 嵌套 ForEach diff 不稳定的问题
- `@Trace dataVersion` 作为原始类型属性，比数组 `@Trace` 更可靠地触发响应式更新
- 条件渲染 (`if/else`) 与 `Tabs` 容器在响应式传播上存在差异，需额外保障

---

## 补充 v3：深度审查 + 编译尝试

- [x] 深度审查平板/PC 响应式链路（3 层分析）
  1. **Index → ClipboardPage 参数传递**：`this.clipboardVM` 是 Index 的普通字段（非 @Local/@Trace），但作为 `@Param` 传给 `ClipboardPage` 时，ArkUI V2 通过 `@ObservedV2` 内部追踪 `@Trace` 属性。链路本身正确，但条件渲染 (`if (this.currentTab === 0)`) 下 ArkUI 的依赖注册可能不如 `Tabs` 容器稳定 → `dataVersion` 兜底
  2. **ClipboardPage 内联布局**：sm 和 md/lg 分支都是内联在 `build()` 中的，`this.vm.clips` / `this.vm.dataVersion` 均在 `build()` 顶层被读取，依赖注册正确
  3. **SettingsPage / CategoryPage**：SettingsPage 无嵌套 ForEach（`Row + 2 Column`），不受影响；CategoryPage 虽有类似 `left/right` 拆分但用户确认无误

- [x] 尝试命令行编译
  - 找到 `hvigorw` 位置：`D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat`
  - 缺少 JDK（DevEco Studio 自带 JDK 未安装在系统 PATH）→ 无法命令行编译
  - 结论：**必须在 DevEco Studio IDE 中编译验证**

- [x] 静态审查结论
  - `void(this.vm.dataVersion)` 语法：ArkTS 基于 TypeScript，`void` 运算符标准支持
  - `Flex + FlexWrap.Wrap` 组件：ArkUI 内置组件，无需额外 import
  - `getCardWidth()` 返回 `'50%'` / `'33.33%'`：百分比宽度在 Flex 子元素中合法
  - 响应式优化模式（乐观更新）不变：所有写操作先同步更新 `fullClips` → `applyFilters()` → 异步写 DB
  - 所有改动文件：仅 2 个（ClipboardViewModel.ets、ClipboardPage.ets）

## 当前状态

| 项目 | 状态 |
|------|:----:|
| 代码改动完成 | ✅ |
| 静态语法审查 | ✅ |
| 命令行编译 | ❌ 缺 JDK |
| IDE 编译验证 | ⏳ 待做 |
| 平板模拟器测试 | ⏳ 待做 |
| 手机回归测试 | ⏳ 待做 |
| PC 模拟器测试 | ⏳ 平板通过后做 |

## 下一步行动

1. 在 DevEco Studio 中打开项目 → Build → Run on Tablet Emulator
2. 测试搜索：输入关键词 → 确认列表即时过滤（不再需要切换 Tab）
3. 测试置顶/取消置顶：长按 → 置顶 → 确认卡片即时移到顶部
4. 测试移动分类：长按 → 移动到分类 → 确认标签即时更新
5. 测试删除：长按 → 删除 → 确认卡片即时消失
6. 手机回归：切换到 Phone Emulator 确认改动未破坏手机布局
7. PC 测试：切换到 2in1 Emulator，确认同样即时响应

---

## 补充 v4（同日）：PC 右键菜单 + 预览面板

- [x] ClipCard 新增 `@Event onPreview` + 双击手势 `TapGesture({ count: 2 })`
  - 单击 → onCopy 复制（不变）
  - 双击 → onPreview 打开预览面板
  - 文件：ClipCard.ets

- [x] ClipboardPage LG 路径修复 + 新增预览面板
  - **右键修复**：LG → `ResponseType.RightClick`，MD → `ResponseType.LongPress`（不变）
  - **预览面板**（LG 专属）：右侧 300vp 固定面板
    - 完整内容（文字全文/图片大图）
    - 元信息：分类标签、时间、置顶状态
    - 操作按钮：复制、置顶/取消置顶、删除、关闭（✕）
    - Escape 键关闭预览
  - 文件：ClipboardPage.ets（+160 行，新增 3 个 @Local + 2 个辅助方法）

- [x] **编译通过** ✅：BUILD SUCCESSFUL in 1.5s，零错误
  - 仅 DatabaseHelper.ets WARN（预存）+ `packing` deprecation（预存）

### 交互对照表

| 操作 | 手机 (SM) | 平板 (MD) | PC (LG) |
|------|:--------:|:--------:|:-------:|
| 复制 | 单击卡片 | 单击卡片 | 单击卡片 |
| 右键/长按 | 长按→ActionSheet | 长按→Menu | **右键→Menu** |
| 预览 | — | — | **双击→右侧面板** |
| 关闭预览 | — | — | Escape / 点击✕ |
| 悬停高亮 | — | onHover | onHover |
| 键盘删除 | — | — | Delete 删除悬停卡片 |

### 待验证

- [ ] PC (2in1) 模拟器：右键能弹出菜单
- [ ] PC 模拟器：双击卡片 → 右侧预览面板打开
- [ ] PC 模拟器：预览面板中复制/置顶/删除按钮正常
- [ ] 平板 (MD)：长按菜单不受影响
- [ ] 手机 (SM)：不受影响

---

## 补充 v5（同日）：修复双击 + 卡片放大

- [x] **双击修复**：`onClick` + 独立 `TapGesture({count:2})` → `GestureGroup(GestureMode.Exclusive)`
  - 根因：ArkUI 中 `onClick` 立即响应对单击，不给双击手势留识别时间，`TapGesture({count:2})` 永远等不到第二下
  - 修复：两个 TapGesture(count:1 和 count:2) 放入 Exclusive 互斥组。双击时不会触发单击，单击立即响应
  - 文件：ClipCard.ets

- [x] **卡片放大**（平板/PC 专属，手机不变）
  - 新增 `@Param wideLayout: boolean = false`
  - 8 处三元选择：字号 14→16、最大行 3→5、内边距 14→20、图片 120→160、截断 200→300、时间 11→12、标签 10→12、图标 22→26、间距 4→6
  - ClipboardPage md/lg 分支传入 `wideLayout: true`，SM 分支不变
  - 文件：ClipCard.ets、ClipboardPage.ets

- [x] **编译通过** ✅：BUILD SUCCESSFUL in 1.57s，零错误

---

## 补充 v6（同日）：分类标签换行 + 平板长按 Grid 修复

- [x] **分类标签响应式换行**
  - SM（手机）：保持横向 Scroll，`.scrollBar(BarState.Off)` → `.scrollBar(BarState.Auto)`，用户可见滚动条
  - MD/LG（平板/PC）：`Row > Scroll > Row` → `Flex({ wrap: FlexWrap.Wrap })`，标签自动换行全部可见，移除固定 `.height(44)`
  - 文件：ClipboardPage.ets

- [x] **平板长按手势根因修复 — FlexWrap → Grid**
  - 根因：`Flex({ wrap: FlexWrap.Wrap })` 在 ArkUI 中**阻断子组件触摸手势传播**，导致 `LongPressGesture` 和 `bindContextMenu(LongPress)` 均无法触发
  - SM 用 `Column`（无此问题），LG 用右键（鼠标事件不经触摸链），仅 MD 的长按被 FlexWrap 杀死
  - 修复：MD 和 LG 全部替换为 ArkUI 原生 `Grid` + `GridItem` 组件
    - MD：`Grid` + `columnsTemplate('1fr 1fr')` 双列，`ClipCard.onClick` + `.bindContextMenu(ResponseType.LongPress)` 同在 ClipCard
    - LG：`Grid` + `columnsTemplate('1fr 1fr 1fr')` 三列，保留 Row 预览面板 + `bindContextMenu(RightClick)` + `onHover`
  - 清理：删除无用的 `getCardWidth()` 方法和 `calcGridColumns` 导入
  - 文件：ClipboardPage.ets

- [x] **命令行编译通过** ✅：BUILD SUCCESSFUL in 5.9s，零错误（仅预存 WARN）
