# Echo — 软件功能描述

> 版本 1.4.0 · HarmonyOS NEXT · 历史剪贴板管理 App

---

## 一、产品概述

Echo 是一款运行在华为鸿蒙系统上的**历史剪贴板管理**应用。它能自动记录用户复制的文字和图片，支持分类管理、全文搜索、置顶收藏和存储时效设置。

**核心价值**：每一次复制都是一次发声，Echo 让每一次发声都有回响——你再也不会丢失曾经复制过的内容。

### 技术特性

- 🖥 **一次开发、多端部署**：同一套代码同时适配手机、平板和 2in1 PC
- 🔄 **跨设备自由流转**：剪贴板数据和分类在设备间自动同步，任务可在设备间无缝迁移
- ⚡ **乐观更新**：所有操作即时响应（< 1ms），持久化异步完成不阻塞 UI
- 📱 **后台持续监听**：App 进程存活期间（前台+后台）持续记录剪贴板变化

---

## 二、功能详解

### F1 · 自动记录剪贴板

**触发条件**：App 进程存活期间（前台运行 + 退到后台），自动监听系统剪贴板变化。

**监听机制（双层架构）**：

| 路径 | 说明 |
|------|------|
| 事件驱动 | 注册 `pasteboard.on('update')` 监听器，系统剪贴板变化时即时触发（真机主路径） |
| 定时轮询 | 每 2.5 秒主动检查一次剪贴板（虚拟机兜底，事件可能不触发） |
| 前台恢复 | App 从后台回到前台时立即检查剪贴板，确保不遗漏 |

**去重机制（三层防护）**：

| 层级 | 说明 |
|------|------|
| 内存去重 | `lastContent` 变量记录上一次内容，相同则跳过 |
| 数据库去重 | 相同类型和内容、且 300 秒内已存在的记录不重复存储 |
| 基线机制 | App 启动时先读取剪贴板建立基线（只记不存），后续只保存与基线不同的内容，防止 App 关闭期间剪贴板中的旧内容被误存 |

**支持格式**：
- 📝 **文字**：通过 `getPrimaryText()` 主路径 + `record.plainText` 兜底路径双重提取
- 🖼 **图片**：检测 `record.pixelMap`，保存为 PNG 文件到 App 沙箱目录

---

### F2 · 首页时间线

所有剪贴内容按照**时间倒序**排列，最新复制的内容在最上面。每条内容以**卡片**形式展示。

**卡片内容**：
- 类型图标（📝 文字 / 🖼 图片）
- 内容预览（文字截取前 200 字，图片显示缩略图）
- 复制时间（格式：`MM-DD HH:mm`）
- 所属分类标签（彩色圆角徽章，颜色与分类一致）
- 置顶标记（📌 图标，仅置顶内容显示）

**多端布局**：

| 设备 | 断点 | 列数 | 容器 |
|------|:---:|:---:|------|
| 手机竖屏 | SM (< 600vp) | 1 列 | `Scroll + Column` |
| 平板 / 折叠展开 | MD (600–840vp) | 2 列 | `Grid + columnsTemplate: 1fr 1fr` |
| 平板横屏 / PC | LG (> 840vp) | 3 列 | `Grid + columnsTemplate: 1fr 1fr 1fr` |

**图片支持**：
- 卡片内显示缩略图（手机 120vp 高、平板/PC 160vp 高，Cover 裁剪）
- 分布式场景下，远端设备无法读取图片文件时，显示灰色占位区域 + "图片仅在本设备可用"提示

---

### F3 · 点击复制回剪贴板

**操作**：点击任意卡片。

**行为**：
- 文字内容直接写回系统剪贴板（`MIMETYPE_TEXT_PLAIN`）
- 图片内容从本地文件读取 PNG → 创建 PixelMap → 写入剪贴板（`MIMETYPE_PIXELMAP`）
- 写入前自动设置去重标记，防止自身写入触发重复记录
- 复制成功后弹出"已复制!"提示气泡（2 秒后自动消失）
- 远端设备上的图片（`imageAvailable === false`）无法复制，显示"图片仅在本设备可用"提示

---

### F4 · 全文搜索

**入口**：剪贴板页面顶部的搜索栏。

**搜索行为（leading-edge + trailing 防抖）**：
- **首次击键**：立即响应，用户立刻看到搜索结果
- **连续输入**：停止输入 **200ms** 后用最终关键词再次搜索
- 避免快速打字时频繁触发过滤，保证流畅体验

**搜索规则**：
- 大小写不敏感（自动转小写比较）
- 文字内容：匹配文本正文
- 图片内容：匹配文件名字符串
- 搜索和筛选为**纯内存操作**，不查询数据库，毫秒级响应

**清除搜索**：
- 点击搜索栏右侧 `✕` 按钮
- 或执行"清空所有内容"操作时自动清除

---

### F5 · 置顶 / 取消置顶

**操作**：

| 设备 | 操作方式 |
|------|------|
| 手机 | 长按卡片 → ActionSheet → 选择"置顶" / "取消置顶" |
| 平板 | 点击 `⋯` 按钮 → ActionSheet → 选择"置顶" / "取消置顶" |
| PC | 右键卡片 → 右键菜单 → 选择"置顶" / "取消置顶" |

**行为**：
- 置顶内容始终排在所有非置顶内容的最前面
- 多个置顶内容之间仍保持时间倒序
- 置顶状态即时切换（乐观更新，先刷新 UI 再异步持久化）

---

### F6 · 删除

**操作方式**：

| 设备 | 操作 |
|------|------|
| 手机 / 平板 | 长按 / 点 `⋯` → ActionSheet → "删除" |
| PC | 右键 → 菜单 → "删除"；或选中卡片后按 `Delete` 键 |

**行为**：
- 即时从列表中移除（乐观更新）
- 删除图片内容时同步清理磁盘上的 PNG 文件
- 支持"清空所有内容"（设置页 → 一键清空 → 确认弹窗）

---

### F7 · 分类管理

**入口**：底部 Tab "分类管理"（手机）/ 左侧导航 "📂 分类管理"（平板/PC）。

**功能**：

| 操作 | 说明 |
|------|------|
| 新建分类 | 点击"＋ 新建分类" → 输入名称 → 选择颜色 → 确认 |
| 编辑分类 | 点击分类右侧 ✎ → 修改名称 / 颜色 → 确认 |
| 删除分类 | 点击分类右侧 🗑 → 确认弹窗 → 该分类下的内容自动移至"默认"分类 |
| 默认分类 | 系统内置，不可删除、不可编辑 |

**颜色系统**：8 种预设颜色可选（珊瑚红、青绿、暖黄、紫罗兰、薄荷绿、粉红、天蓝、橘橙）。新建分类时自动推荐未使用的颜色。

**多端布局**：

| 设备 | 布局 |
|------|------|
| 手机 SM | 单列全宽行 |
| 平板/PC MD/LG | 双列卡片网格 |

---

### F8 · 移动到分类

**操作**：长按 / 点 `⋯` / 右键 → ActionSheet / 菜单 → "移动到..." → 弹出分类选择弹窗 → 点击目标分类。

**行为**：
- 卡片上的分类标签即时更新为新分类的颜色和名称
- 一条内容只属于一个分类（一对一关系）
- 移动后，在当前分类筛选下可能不再可见（因为已不在当前筛选项）

---

### F9 · 分类筛选

**入口**：剪贴板页面搜索栏下方的分类标签栏。

**标签项**：
- "全部"（显示所有分类的内容）
- 每个自定义分类（显示分类名 + 颜色圆点）

**行为**：
- 点击标签 → 仅显示该分类下的内容卡��
- 点击"全部" → 恢复显示全部内容
- 当前选中的标签以**品牌色填充 + 白色文字**高亮

**多端适配**：

| 设备 | 标签栏行为 |
|------|------|
| 手机 SM | 横向滚动（滚动条可见） |
| 平板/PC MD/LG | 自动换行（FlexWrap），全部标签始终可见 |

---

### F10 · 存储时效

**入口**：设置页。

**选项**：

| 选项 | 行为 |
|------|------|
| 永不 | 内容永久保留，不过期 |
| 1 天 | 24 小时后自动清理 |
| 3 天 | 72 小时后自动清理（默认） |
| 5 天 | 120 小时后自动清理 |
| 7 天 | 168 小时后自动清理 |

**清理机制**：
- App 启动时自动执行 `cleanExpired()`，清理所有超过时效的内容
- 被清理的图片内容同步删除磁盘文件
- 切换 Tab 到剪贴板时也会触发清理

---

### F11 · 清空所有

**入口**：设置页 → "清空所有内容"。

**行为**：
- 弹出确认弹窗（"确认清空所有剪贴板内容？此操作不可撤销"）
- 确认后即时清空列表 + 数据库（乐观更新，先清内存再异步清 DB）
- 同时重置搜索关键词和分类筛选状态
- 不清除分类数据

---

## 三、多端交互对照

### 导航方式

| 设备 | 导航 | 说明 |
|------|------|------|
| 手机 SM | 底部 Tab（📋 剪贴板 / 📂 分类 / ⚙ 设置） | 跟随系统导航栏 |
| 平板 MD | 左侧图标导航（宽 60vp） | 仅显示 emoji 图标，居中排列 |
| PC LG | 左侧图标+文字导航（宽 180vp） | 显示品牌名 "Echo" + emoji + 中文文字 |

### 卡片操作对照

| 操作 | 手机 (SM) | 平板 (MD) | PC / 2in1 (LG) |
|------|:--------:|:--------:|:-------:|
| 复制到剪贴板 | 单击卡片 | 单击卡片 / 点 `⋯` → ActionSheet | 单击卡片 |
| 弹出操作菜单 | 长按 → ActionSheet | 点 `⋯` 按钮 → ActionSheet / 长按 → Menu | 右键 → Menu / 点 `⋯` → ActionSheet |
| 预览大图 / 全文 | — | 双击卡片 → 右侧预览面板 | 双击卡片 → 右侧预览面板 |
| 关闭预览 | — | Escape 键 / 点击 ✕ | Escape 键 / 点击 ✕ |
| 悬停高亮 | — | onHover → 阴影 + 缩放 | onHover → 阴影 + 缩放 |
| 键盘删除 | — | — | 悬停卡片上按 Delete 键 |

### 分类标签栏

| 设备 | 行为 |
|------|------|
| 手机 SM | 横向滚动条，左右滑动查看所有标签 |
| 平板/PC MD/LG | FlexWrap 自动换行，所有标签始终完全可见 |

---

## 四、跨设备自由流转

### 4.1 分布式 RDB 数据同步

当用户拥有多台 HarmonyOS 设备（登录同一华为账号、开启 Wi-Fi 和蓝牙）时：

- **剪贴板记录**和**分类数据**自动在设备间同步
- 设备 A 上记录的复制内容，在设备 B 上打开 Echo 即可看到
- 设备 A 上创建的分类，设备 B 上自动出现
- 同步延迟通常在数秒内
- 图片文件的路径会同步，但**图片文件本身不跨设备传输**（远端显示占位提示）

### 4.2 跨端任务迁移

用户可以从一台设备**无缝切换到另一台设备**继续使用：

1. 在设备 A 上搜索某个关键词、筛选某个分类
2. 触发跨端迁移（系统能力，需要双端同一华为账号）
3. 设备 B 上 Echo 自动恢复：
   - 当前所在页面（剪贴板 / 分类 / 设置）
   - 搜索关键词
   - 分类筛选状态

**举个例**：你在手机上搜索"会议纪要"，然后迁移到平板——平板上 Echo 自动打开并已在搜索"会议纪要"，无需重复操作。

---

## 五、权限说明

Echo 需要以下权限才能正常工作：

| 权限 | 用途 | 类型 | 请求时机 |
|------|------|:---:|------|
| `ohos.permission.READ_PASTEBOARD` | 读取系统剪贴板的内容 | user_grant | App 首次启动时弹窗请求 |
| `ohos.permission.DISTRIBUTED_DATASYNC` | 跨设备数据自动同步 | user_grant | App 首次启动时弹窗请求 |
| `ohos.permission.INTERNET` | 基础网络访问 | normal | 自动授予 |

**权限被拒时的降级行为**：
- `READ_PASTEBOARD` 被拒 → 剪贴板监听不启动，App 仍可正常打开和使用，但无法自动记录新内容
- `DISTRIBUTED_DATASYNC` 被拒 → 分布式同步静默关闭，App 以纯本地模式运行，不影响其他功能

---

## 六、已知限制与注意事项

| 项目 | 说明 |
|------|------|
| 图片粘贴 | 重启 App 后图片粘贴功能偶发不稳定（HarmonyOS 沙箱文件路径兼容性问题） |
| 悬浮窗 | 系统级悬浮窗在 HarmonyOS NEXT 上不向第三方 App 开放 |
| 跨端迁移 | 需要双端同一华为账号、Wi-Fi + 蓝牙开启、均为 HarmonyOS NEXT Release 及以上版本 |
| 分布式图片 | 图片文件路径会同步，但文件本身不跨设备传输（远端显示占位提示） |
| 后台运行 | 后台监听依赖 App 进程存活；如被系统杀死则停止记录 |

---

## 七、完整功能矩阵

| # | 功能 | 手机 | 平板 | PC | 状态 |
|:---:|------|:---:|:---:|:---:|:---:|
| F1 | 自动记录剪贴板 | ✅ | ✅ | ✅ | 已实现 |
| F2 | 时间线首页 | ✅ | ✅ | ✅ | 已实现 |
| F3 | 点击复制 | ✅ | ✅ | ✅ | 已实现 |
| F4 | 搜索 | ✅ | ✅ | ✅ | 已实现 |
| F5 | 置顶 | ✅ | ✅ | ✅ | 已实现 |
| F6 | 删除 | ✅ | ✅ | ✅ | 已实现 |
| F7 | 分类管理 | ✅ | ✅ | ✅ | 已实现 |
| F8 | 移动到分类 | ✅ | ✅ | ✅ | 已实现 |
| F9 | 分类筛选 | ✅ | ✅ | ✅ | 已实现 |
| F10 | 存储时效 | ✅ | ✅ | ✅ | 已实现 |
| F11 | 清空所有 | ✅ | ✅ | ✅ | 已实现 |
| — | 图片记录与复制 | ✅ | ✅ | ✅ | 已实现 |
| — | 多端响应式布局 | ✅ | ✅ | ✅ | 已实现 |
| — | 分布式数据同步 | ✅ | ✅ | ✅ | 已实现 |
| — | 跨端任务迁移 | ✅ | ✅ | ✅ | 已实现 |
| — | 右键菜单 | — | — | ✅ | 已实现 |
| — | 悬停高亮 | — | ✅ | ✅ | 已实现 |
| — | 双击预览面板 | — | ✅ | ✅ | 已实现 |
| — | 键盘快捷键 | — | — | ✅ | 已实现 |
| — | 平板 `⋯` 操作按钮 | — | ✅ | ✅ | 已实现 |
