# Echo — 工程代码解析

> 版本 1.4.0 · HarmonyOS NEXT · ArkTS · 16 个源文件

---

## 一、项目总览

Echo 是一款运行在华为鸿蒙系统上的历史剪贴板管理 App。工程共 **16 个 `.ets` 源文件**，分属 7 个目录，外加 3 个配置文件。总体遵循 **MVVM 分层架构**：Model → ViewModel → View（Pages + Components），数据持久化通过 DatabaseHelper 单例完成。

```
用户操作 / 系统事件
      ↓
ViewModel（@ObservedV2 响应式状态）
      ↓  @Trace 属性变更
Pages（@ComponentV2 UI 渲染）
      ↓  用户交互回调
ViewModel 业务方法
      ↓
DatabaseHelper CRUD（异步持久化）
```

### 文件清单（按职责分层）

| 层 | 文件 | 行数 | 职责 |
|------|------|:---:|------|
| **入口** | `entryability/EntryAbility.ets` | 75 | UIAbility 生命周期、跨端迁移 |
| **模型** | `model/ClipItem.ets` | 14 | 剪贴内容数据结构 |
| | `model/CategoryItem.ets` | 8 | 分类数据结构 |
| **数据** | `database/DatabaseHelper.ets` | 297 | RDB 增删改查、分布式同步 |
| **业务** | `viewmodel/ClipboardViewModel.ets` | 643 | 剪贴板监听、数据操作、筛选 |
| | `viewmodel/CategoryViewModel.ets` | 121 | 分类 CRUD、乐观更新 |
| **页面** | `pages/Index.ets` | 208 | 导航容器、断点监听、迁移恢复 |
| | `pages/ClipboardPage.ets` | ~700 | 剪贴板首页（卡片列表/网格） |
| | `pages/CategoryPage.ets` | 445 | 分类管理页 |
| | `pages/SettingsPage.ets` | ~170 | 设置页 |
| **组件** | `component/ClipCard.ets` | 143 | 剪贴内容卡片 |
| | `component/SearchBar.ets` | 43 | 搜索栏（受控组件） |
| | `component/SideNav.ets` | 85 | 侧边导航栏 |
| **公共** | `common/BreakpointConstants.ets` | 84 | 响应式断点系统 |
| | `common/ContinuationBridge.ets` | 77 | 跨端迁移状态桥接 |
| **常量** | `constant/CommonConstants.ets` | 50 | 颜色、配置常量 |

---

## 二、数据模型（Model）

### ClipItem — 剪贴内容

```typescript
// model/ClipItem.ets
export class ClipItem {
  id: number = 0;                    // 自增主键（DB 返回）
  type: string = 'text';            // 'text' | 'image'
  content: string = '';             // 文本内容 或 图片文件路径
  categoryId: number = 1;           // 所属分类 ID
  isPinned: boolean = false;        // 是否置顶
  createdAt: number = Date.now();   // 创建时间戳（ms）
  expiresAt: number = 0;            // 过期时间戳，0=永不过期
  imageAvailable: boolean = true;   // 分布式场景：图片文件是否在本地设备可用
}
```

**关键设计点**：
- `content` 字段复用于文本和图片路径，通过 `type` 区分
- `imageAvailable` 是分布式场景特有字段：RDB 只同步路径字符串，远端设备上 `fileIo.statSync` 检测不到文件时置 `false`
- 无 `@ObservedV2` 装饰——ClipItem 不作为响应式根对象，响应式由 ViewModel 的数组引用变更驱动

### CategoryItem — 分类

```typescript
// model/CategoryItem.ets
export class CategoryItem {
  id: number = 0;
  name: string = '';
  color: string = '#FF6B6B';
  createdAt: number = Date.now();
}
```

---

## 三、数据库层（DatabaseHelper）

`database/DatabaseHelper.ets` — 单例模式，封装全部 RDB 操作。

### 3.1 初始化与分布式配置

```typescript
static getInstance(): DatabaseHelper { ... }  // 单例

async init(context): Promise<void> {
  this.store = await relationalStore.getRdbStore(context, STORE_CONFIG);
  await this.store.executeSql(SQL_CREATE_CATEGORIES);
  await this.store.executeSql(SQL_CREATE_CLIPS);
  await this.ensureDefaultCategory();

  // 分布式同步
  await this.store.setDistributedTables(
    ['clips', 'categories'],
    relationalStore.DistributedType.DISTRIBUTED_DEVICE,
    { autoSync: true }
  );
}
```

**关键决策**：
- `securityLevel: S3` — 分布式 RDB 要求的最低安全等级
- `autoSync: true` — 表数据自动跨设备同步，无需手动触发
- 异常静默降级：`setDistributedTables` 在不支持分布式的环境（模拟器、权限被拒）会抛异常，由 try-catch 吞掉，不影响本地功能

### 3.2 去重机制

```typescript
// addClip 中的去重查询（300s 窗口）
dupPred.equalTo('content', clip.content);
dupPred.equalTo('type', clip.type);
dupPred.greaterThan('createdAt', Date.now() - 300000); // 300s
```

去重窗口从原始的 60s 延长到 300s，以覆盖分布式同步延迟场景：设备 A 写入后，设备 B 可能也检测到同一剪贴板事件并延时写入。

### 3.3 分布式变更监听

```typescript
onDistributedDataChange(callback): void {
  this.store.on('dataChange', relationalStore.SubscribeType.SUBSCRIBE_TYPE_REMOTE, callback);
}

offDistributedDataChange(callback): void {
  this.store.off('dataChange', relationalStore.SubscribeType.SUBSCRIBE_TYPE_REMOTE, callback);
}
```

ViewModel 在 `init()` 时注册监听，`release()` 时注销。远程数据到达 → 回调触发 → `loadClips()` 全量刷新。

---

## 四、业务逻辑层（ViewModel）

### 4.1 ClipboardViewModel — 核心业务（643 行，最大文件）

#### 响应式状态

```typescript
@ObservedV2
export class ClipboardViewModel {
  @Trace clips: ClipItem[] = [];         // UI 展示列表（过滤后）
  @Trace searchKeyword: string = '';     // 当前搜索词
  @Trace selectedCategoryId: number = 0; // 当前分类筛选（0=全部）
  @Trace retentionDays: number = 3;      // 存储时效
  @Trace dataVersion: number = 0;       // 数据版本计数器（条件渲染依赖）
  private fullClips: ClipItem[] = [];    // 全量主列表（私有，不过滤）
}
```

#### 初始化流程

```
init(context)
  ├── db.init()            → RDB 初始化 + setDistributedTables
  ├── ensureDefaultCategory()
  ├── loadRetentionSetting() → Preferences 读取
  ├── cleanExpired()         → 清理过期内容
  ├── loadClips()            → DB → fullClips → applyFilters() → clips
  ├── requestPermission()    → 弹窗请求 READ_PASTEBOARD + DISTRIBUTED_DATASYNC
  └── startMonitoring()      → 启动双层监听
      ├── pasteboard.on('update') → 事件驱动
      ├── establishBaseline()    → 基线机制
      └── scheduleNextPoll()     → 2.5s 轮询
```

#### 权限请求（2026-06-22 补全）

```typescript
private async requestPermission(): Promise<void> {
  // 检查两个权限的授权状态
  const pasteboardGranted = checkAccessTokenSync(READ_PASTEBOARD);
  const distributedGranted = checkAccessTokenSync(DISTRIBUTED_DATASYNC);
  // 收集未授权权限 → 一次性弹窗请求
  const ungranted = [];
  if (!pasteboardGranted) ungranted.push(READ_PASTEBOARD);
  if (!distributedGranted) ungranted.push(DISTRIBUTED_DATASYNC);
  if (ungranted.length > 0) {
    await requestPermissionsFromUser(ungranted);
  }
}
```

与最初只请求 `READ_PASTEBOARD` 相比，现在两个 `user_grant` 权限合并为一次弹窗。

#### 双层监听架构

| 路径 | 触发方式 | 适用场景 |
|------|----------|----------|
| 事件驱动 | `pasteboard.on('update')` | 真机主路径 |
| 轮询兜底 | `setTimeout` 2.5s 递归 | 虚拟机（事件可能不触发） |
| 前台恢复 | `onPageShow → onForeground()` | App 从后台回到前台 |

#### 基线机制

```
needsBaseline = true（启动时）
      ↓
首次读到剪贴板
      ↓
只记入 lastContent，不存 DB
      ↓  needsBaseline = false
后续读到 → saveClip() 正常存储
```

防止 App 关闭期间剪贴板里的旧内容被误存为"新复制"。

#### 数据流：saveClip

```
readAndSaveClipboard()
  ↓ getPrimaryText() / record.plainText 双重提取
  ↓
saveClip(type, content)
  ├── 内存去重: content === lastContent ? → skip
  ├── lastContent = content
  ├── seq = ++saveSeq（竞态保护序列号）
  ├── await db.addClip(clip)  // 异步写 DB
  ├── seq !== saveSeq ? → skip（被新调用覆盖，丢弃）
  └── fullClips.prepend(clip) → applyFilters() → clips 更新
```

#### 乐观更新模式

所有写操作（`togglePin`、`moveToCategory`、`deleteClip`、`deleteAll`）遵循统一模式：

```
1. 创建新对象 → 更新 fullClips → applyFilters()（同步，< 1ms 响应）
2. await db.xxx()（异步持久化，不阻塞 UI）
```

#### 分布式数据同步

```
远程设备数据变更
  ↓
store.on('dataChange', SUBSCRIBE_TYPE_REMOTE)
  ↓
saveSeq++（中断进行中的本地 saveClip 竞态）
  ↓
loadClips() → fullClips 全量刷新 → applyFilters()
```

#### 搜索防抖（leading-edge + trailing）

```typescript
onSearch(keyword):
  if (searchTimer === -1) {
    // Leading-edge: 首次击键立即搜索
    searchKeyword = keyword; applyFilters();
  }
  clearTimeout(searchTimer);
  searchTimer = setTimeout(() => {
    // Trailing: 停止输入 200ms 后最终搜索
    searchKeyword = keyword; applyFilters();
  }, 200);
```

#### ForEach Key 策略（2026-06-22 修复）

ClipboardPage 中三处 ForEach key 均为：
```
clip.id + '_' + clip.categoryId + '_' + (isPinned ? '1' : '0')
```

包含三个可变维度，确保移动分类或置顶切换时 key 变更 → DOM 强制重建 → 即时刷新。

### 4.2 CategoryViewModel — 分类管理（121 行）

```typescript
@ObservedV2
export class CategoryViewModel {
  @Trace categories: CategoryItem[] = [];
}
```

与 ClipboardViewModel 相同的模式：
- 乐观更新（`addCategory` / `updateCategory` / `deleteCategory`）
- 分布式变更监听（远程数据到达 → `loadCategories()`）
- `addCategory` 中临时负 id → DB 返回真实 id 后替换

---

## 五、UI 层（Pages + Components）

### 5.1 Index — 导航容器（208 行）

`pages/Index.ets` — `@Entry` 根组件，负责：

1. **断点监听**：`aboutToAppear` 主动获取窗口宽度 → `calcBreakpoint()`，`onAreaChange` 持续监听
2. **响应式导航切换**：
   - SM（手机）：`Tabs` 底部导航
   - MD/LG（平板/PC）：`SideNav` 侧边导航 + 条件渲染内容区
3. **跨端迁移恢复**：`checkAndRestoreContinuation()` 消费 `ContinuationBridge` 待恢复数据
4. **Tab 状态同步**：每次切换更新 `ContinuationBridge.instance.currentTab`

```typescript
// 冷启动恢复
async aboutToAppear(): Promise<void> {
  await this.categoryVM.init(context);
  await this.clipboardVM.init(context);
  this.checkAndRestoreContinuation();  // 消费迁移数据
}

// 热启动恢复
onPageShow(): void {
  this.clipboardVM.onForeground();
  this.checkAndRestoreContinuation();
}
```

### 5.2 ClipboardPage — 剪贴板首页（~700 行，最大页面）

三套布局按断点切换：

| 断点 | 布局 | 列数 | 交互 |
|------|------|:---:|------|
| SM | `Scroll > Column > ForEach` 单列 | 1 | 单击复制、长按 ActionSheet |
| MD | `Grid > GridItem` 双列 | 2 | 单击复制、`⋯` 按钮 ActionSheet、长按 Menu |
| LG | `Grid > GridItem` 三列 + 预览面板 | 3 | 单击复制、右键 Menu、双击预览、悬停高亮 |

#### 分类标签栏

- SM：`Scroll > Row` 横向滚动
- MD/LG：`Flex({ wrap: FlexWrap.Wrap })` 自动换行

#### ActionSheet 处理

```typescript
// 从 clips 实时查找最新引用，避免参数对象过期
private getSelectedClipActual(): ClipItem | null {
  for (const c of this.vm.clips) {
    if (c.id === this.selectedClip!.id) return c;
  }
  return null;
}
```

#### 平板"⋯"按钮

ClipCard 使用 `Stack({ alignContent: Alignment.TopEnd })` 在右上角浮动 `⋯`。文字内容区通过 `Column.layoutWeight(1).margin({ right: 36 })` 预留空间，避免与 `⋯` 重叠。

#### PC 预览面板

- 双击卡片 → 右侧 `width('30%')` 面板
- `showActions: !this.showPreview` — 预览打开时隐藏 `⋯` 避免拥挤
- Escape 关闭、Delete 删除悬停卡片

### 5.3 CategoryPage — 分类管理（445 行）

- SM：单列全宽行
- MD/LG：双列卡片网格（奇偶拆分到左右列）

#### 颜色选择

8 种预设颜色。SM 下每个圆圈 32×32 vp（防止溢出 85% 宽弹窗），MD/LG 下 40×40 vp。

#### ForEach Key（2026-06-22 修复）

```
cat.id + '_' + cat.name + '_' + cat.color
```

名称或颜色变更 → key 变更 → DOM 重建 → 即时刷新。

### 5.4 SettingsPage — 设置页（~170 行）

- 存储时效选择器（永不/1/3/5/7 天）
- 一键清空确认弹窗
- 关于信息（版本号、技术栈）

### 5.5 ClipCard — 剪贴内容卡片（143 行）

`@ComponentV2` 组件，通过 `@Param` 接收数据，无内部状态。

**响应式参数**：

| @Param | 类型 | 说明 |
|------|------|------|
| `clipItem` | ClipItem | 剪贴内容数据 |
| `categoryName` | string | 分类名称（由父组件查 CategoryVM 后传入） |
| `categoryColor` | string | 分类颜色 |
| `isHovered` | boolean | PC 悬停状态 |
| `wideLayout` | boolean | 是否宽屏布局（平板/PC） |
| `showActions` | boolean | 是否显示 ⋯ 按钮 |

**布局结构**：

```
Stack(TopEnd)
├── Column                  ← 卡片主体（clip(true) 圆角裁剪）
│   └── Row
│       ├── Text(emoji)     ← 类型图标
│       ├── Column(layoutWeight:1, margin-right:36)
│       │   ├── Image / Text   ← 内容
│       │   └── Row            ← 时间 + 分类标签 + 置顶标记
│       └── (gap for ⋯)
└── Text('⋯')               ← 浮动操作按钮（仅 wideLayout && showActions）
```

**尺寸适配**：通过 `wideLayout` 三元表达式控制 8 处差异（图标 22→26、图片高 120→160、文字 14→16、最大行数 3→5 等）。

**PC 悬停效果**：`isHovered` → `shadow(8px)` + `scale(1.02)` + `animation(200ms)`。

**分布式图片占位**：`imageAvailable === false` 时显示灰色区域 + "图片仅在本设备可用"。

### 5.6 SearchBar — 搜索栏（43 行）

**受控组件模式**：自身不持有输入状态，通过 `@Param value` + `@Event onChange` 与父组件双向绑定。

```
SearchBar({ value: this.searchText })
  .onChange((val) => { this.vm.onSearch(val); })
```

`✕` 清除按钮仅在 `value.length > 0` 时显示，点击触发 `onChange('')`。

### 5.7 SideNav — 侧边导航栏（85 行）

| 断点 | 宽度 | 标题 | 导航项 |
|------|:---:|------|------|
| MD | 60vp | 📋 图标 | 仅图标，居中 |
| LG | 180vp | "Echo" 文字 | 图标 + 文字，左对齐 |

选中态：`backgroundColor('#FFF0F0')` + 文字加粗 + 品牌色。

---

## 六、公共模块（Common）

### 6.1 BreakpointConstants — 响应式断点系统（84 行）

```typescript
export enum Breakpoint { SM = 'sm', MD = 'md', LG = 'lg' }

calcBreakpoint(width) → width < 600 → SM
                      → width ≤ 840 → MD
                      → width > 840  → LG

calcGridColumns(bp) → 1 / 2 / 3
isWideLayout(bp)    → bp === MD || bp === LG
sideNavWidth(bp)     → MD: 60, LG: 180
```

断点传递方式：`@Param` 显式向下传递（非全局状态），符合 ArkTS 严格模式。

### 6.2 ContinuationBridge — 跨端迁移桥接（77 行）

**单例模式**，连接 UIAbility（非 UI 上下文）与 Index（`@ComponentV2`）之间的状态传递。

```
┌─── 源端 ───┐                    ┌─── 对端 ───┐
Index/VM 更新桥接字段                EntryAbility
  currentTab                       onCreate/onNewWant
  searchKeyword         →          restoreFromWant()
  selectedCategoryId                  ↓
         ↓                     Index.checkAndRestoreContinuation()
EntryAbility.onContinue()        → consumeIfPending()
  snapshotToWantParam()           → 恢复 Tab/搜索/筛选
```

**数据量控制**：仅传递 3 个字段（~200 字节），远小于 `wantParam` 100KB 限制。剪贴内容和分类列表由分布式 RDB autoSync 负责。

---

## 七、入口文件（EntryAbility）

`entryability/EntryAbility.ets` — 75 行，实现 HarmonyOS 四个关键生命周期：

| 生命周期 | 职责 |
|----------|------|
| `onCreate` | 检查 `launchReason === CONTINUATION` → 冷启动迁移恢复 |
| `onContinue` | 源端快照 `currentTab/searchKeyword/selectedCategoryId` → `wantParam` → 返回 `AGREE` |
| `onNewWant` | 热启动迁移恢复（App 已在运行） |
| `onWindowStageCreate` | 加载 `pages/Index` 主页面 |

---

## 八、配置文件

### module.json5 — 模块描述

| 配置项 | 值 | 说明 |
|------|------|------|
| `deviceTypes` | `["phone", "tablet", "2in1"]` | 三端部署 |
| `continuable` | `true` | 启用跨端迁移 |
| 权限 | `INTERNET` / `READ_PASTEBOARD` / `DISTRIBUTED_DATASYNC` | 3 项权限 |

### build-profile.json5

```
compatibleSdkVersion: 5.0.5(17)
targetSdkVersion: 6.0.0(20)
runtimeOS: HarmonyOS
```

### app.json5

```
versionCode: 1004000
versionName: 1.4.0
bundleName: com.example.echo
```

---

## 九、核心架构决策汇总

### 9.1 数据流：DB = 仅持久化，内存 = UI 唯一数据源

```
fullClips（私有，全量主列表）
    ↓  applyFilters()
    ↓  按 searchKeyword / selectedCategoryId 过滤
    ↓  pinned 优先排序
    ↓
clips（@Trace，UI 展示列表）
    ↓
ForEach 渲染卡片
```

搜索和筛选是纯内存操作，不查 DB。

### 9.2 乐观更新（Optimistic Update）

所有写操作（增/删/改/置顶/移分类）遵循"先内存后 DB"原则：
- 同步更新 fullClips → applyFilters() → UI 即时响应（< 1ms）
- 异步 await db.xxx() → 持久化（50-200ms 延迟对用户不可见）

### 9.3 ForEach Key 策略

| 页面 | Key | 包含维度 |
|------|-----|------|
| ClipboardPage（三处） | `id + categoryId + isPinned` | 移动分类 + 置顶切换后强制刷新 |
| CategoryPage（三处） | `id + name + color` | 名称/颜色修改后强制刷新 |

### 9.4 去重机制（三层防护）

| 层级 | 机制 | 窗口 |
|------|------|:---:|
| 内存级 | `lastContent` 字符串比较 | 实时 |
| 数据库级 | `content + type + createdAt` 查询 | 300s |
| 竞态保护 | `saveSeq` 序列号 | 异步间隙 |

### 9.5 自由流转双层架构

| 层次 | 版本 | 机制 | 同步内容 |
|------|:---:|------|------|
| 分布式 RDB | v7 | `setDistributedTables` + autoSync | clips / categories 表数据 |
| 跨端迁移 | v8 | `onContinue` + `ContinuationBridge` | currentTab / searchKeyword / selectedCategoryId |

---

## 十、已知技术债务

1. **图片粘贴偶发不稳定** — `MIMETYPE_PIXELMAP` 兼容性问题（2026-05-20 记录）
2. **PackageHap 需 Java 环境** — `spawn java ENOENT` 需配置 JDK
3. **分布式图片不同步** — RDB 仅同步路径，文件本身不跨设备传输
4. **跨端迁移需双设备验证** — 代码已完整，待真机测试
5. **SettingsPage 版本号硬编码** — 需手动与 `app.json5` 同步
